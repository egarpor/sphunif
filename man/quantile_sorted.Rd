% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auxiliary.R
\name{quantile_sorted}
\alias{quantile_sorted}
\alias{quantile_col}
\title{Type-7 quantiles of a vector}
\usage{
quantile_sorted(x_sorted, probs)

quantile_col(x, probs)
}
\arguments{
\item{x_sorted}{a vector \bold{sorted increasingly} and \bold{free of}
\code{NA}s.}

\item{probs}{a vector of probabilities in \eqn{[0, 1]}.}

\item{x}{a vector, possibly unsorted or containing \code{NA}s.}
}
\value{
A vector of size \code{length(probs)} with the type-7 quantiles.
}
\description{
Computation of the type-7 quantiles of a vector, avoiding
sorting if observations are already sorted.
}
\details{
\code{quantile_sorted} reproduces \code{quantile(x_sorted, probs,
type = 7, names = FALSE)} bit-for-bit, as it mirrors the interpolation in
\code{stats:::quantile.default} (including its tie guard), but without
re-sorting the vector.

\code{quantile_col} takes the \code{quantile_sorted} shortcut only when
\code{x} is sorted increasingly and free of \code{NA}s, and delegates to
\code{\link[stats]{quantile}} otherwise.
}
\section{Warning}{

\code{quantile_sorted} does \bold{not} check its assumptions on
\code{x_sorted}, and it returns spurious results if \code{x_sorted} is
unsorted or contains \code{NA}s. Use \code{quantile_col} if these
assumptions cannot be guaranteed.
}

\examples{
# quantile_sorted() requires a sorted and NA-free vector
x_sorted <- sort(rnorm(50))
sphunif:::quantile_sorted(x_sorted = x_sorted, probs = c(0.25, 0.5, 0.95))
quantile(x_sorted, probs = c(0.25, 0.5, 0.95), type = 7, names = FALSE)

# quantile_col() delegates to stats::quantile() if unsorted or if NAs
x <- rnorm(50)
sphunif:::quantile_col(x = x, probs = c(0.25, 0.5, 0.95))
sphunif:::quantile_col(x = c(NA, sort(x)), probs = c(0.25, 0.5, 0.95))
}
\keyword{internal}
