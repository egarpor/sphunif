
# test_that("Statistic arguments of unif_stat_distr are contained in those of
#           unif_test", {
#
#   expect_true(all(
#     names(formals(unif_stat_distr))[-c(1:7)] %in% names(formals(unif_test))[-c(1:8)]
#   ))
#
# })
